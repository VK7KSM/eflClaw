/target
/target_ci
/target_review*
firmware/*/target
*.db
*.db-journal
.DS_Store
._*
.wt-pr37/
__pycache__/
*.pyc
docker-compose.override.yml
site/node_modules/
site/.vite/
site/public/docs-content/
gh-pages/

.idea

# Private local config and data — never commit
资料/

# Release artifacts (built locally)
elfClaw-*.zip
zeroclaw-*.zip

# Environment files (may contain secrets)
.env

# Python virtual environments

.venv/
venv/

# ESP32 build cache (esp-idf-sys managed)

.embuild/
.env.local
.env.*.local

# Secret keys and credentials
.secret_key
otp-secret
*.key
*.pem
credentials.json
/config.toml
.worktrees/
备份代码

# Nix
result
